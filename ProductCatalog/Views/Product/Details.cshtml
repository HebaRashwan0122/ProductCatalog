@model ProductCatalog.Service.ViewModels.ProductDetailsViewModel

@* @{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
 *@
<div class="container p-3 my-3 border">
    <h1>Product Details</h1>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Price)
        </dd>

      
        
    </dl>

    @if(Model.Img!=null)
    {
        <img src="data:image/jpeg;base64,@(Convert.ToBase64String(Model.Img))" height="50" width="50">
    }
   
    <div>
@*         <a class="btn btn-primary" asp-action="AddOrEdit" asp-route-employeeId="@Model.EmployeeId">Edit</a>
 *@        <a class="btn btn-danger" asp-action="Index">Back</a>
    </div>
</div>